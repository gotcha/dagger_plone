inputs:
  dagger-nix:
    url: github:dagger/nix
    inputs:
      nixpkgs:
        follows: nixpkgs
  devenv-dagger:
    url: github:jfroche/devenv-dagger
    flake: false
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
imports:
- devenv-dagger/dagger.devenv.nix
- devenv-dagger/podman.devenv.nix
